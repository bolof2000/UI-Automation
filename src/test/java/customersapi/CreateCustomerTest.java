package customersapi;
import io.restassured.response.Response;
import org.junit.Test;
import static io.restassured.RestAssured.*;

public class CreateCustomerTest {

    @Test
    public void validateCreateCustomerAPIithValidCredentials(){
       /* Response response = given().auth().basic(config.getProperty("Access_key"),config.getProperty("Password_key")).
                formParam("email","bolofbaba@gmail.com").
                formParam("description","description of api ").
                formParam("name","Olusegun Bolofinde").
                formParam("address","1714 shea center drive").
                post(config.getProperty("customerAPIEndpoint"));

        response.prettyPrint();


*/


    }
}
